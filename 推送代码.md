# 🚀 推送代码到 GitHub

## 当前状态

✅ 你已经完成：
- Git 仓库已初始化
- 远程仓库已配置：`https://github.com/yuanhao667/exam-index.git`
- 代码已提交到本地

## 📤 现在需要推送代码

在终端中执行以下命令：

```bash
git push -u origin main
```

---

## 🔐 如果要求输入用户名和密码

执行 `git push` 时，可能会要求你输入：

1. **Username（用户名）**：输入 `yuanhao667`
2. **Password（密码）**：⚠️ **不要输入密码**，需要使用 **Personal Access Token**

### 如何创建 Personal Access Token：

1. **打开浏览器，访问**：
   https://github.com/settings/tokens

2. **点击** "Generate new token" > "Generate new token (classic)"

3. **填写信息**：
   - **Note**（备注）：例如 "exam-index project"
   - **Expiration**（过期时间）：选择 "No expiration"（永不过期）
   - **勾选权限**：至少勾选 `repo`（全部仓库权限）

4. **点击** "Generate token"（绿色按钮）

5. **复制生成的 token**：
   - ⚠️ **重要**：这个 token 只显示一次，格式类似：`ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`
   - 一定要复制保存好！

6. **回到终端，当要求输入密码时，粘贴这个 token**（而不是输入密码）

---

## ✅ 验证是否成功

推送成功后，你应该看到类似这样的输出：
```
Enumerating objects: 12, done.
Counting objects: 100% (12/12), done.
Delta compression using up to 8 threads
Compressing objects: 100% (10/10), done.
Writing objects: 100% (12/12), 38.50 KiB | 2.10 MiB/s, done.
Total 12 (delta 1), reused 0 (delta 0), pack-reused 0 (from 0)
remote: Resolving deltas: 100% (1/1), done.
To https://github.com/yuanhao667/exam-index.git
 * [new branch]      main -> main
Branch 'main' set up to track remote branch 'main' from 'origin'.
```

然后：
1. **打开浏览器，访问**：https://github.com/yuanhao667/exam-index
2. **你应该能看到所有文件**：
   - ✅ index.html
   - ✅ DEFAULT_QUESTIONS_ui.js
   - ✅ DEFAULT_QUESTIONS_visual.js
   - ✅ README.md
   - ✅ 其他所有文件

如果看到了这些文件，说明上传成功！🎉

---

## 🆘 如果遇到问题

### 问题 1：`fatal: unable to access 'https://github.com/...'`
**原因**：网络连接问题
**解决**：
- 检查网络连接
- 如果在中国大陆，可能需要使用 VPN 或代理
- 或者尝试使用 SSH 方式（需要配置 SSH key）

### 问题 2：`Permission denied` 或认证失败
**解决**：
- 确保使用 Personal Access Token 而不是密码
- 检查 token 是否有 `repo` 权限
- 确保用户名正确：`yuanhao667`

### 问题 3：`error: failed to push some refs`
**解决**：可能是远程仓库已经有内容，尝试：
```bash
git push -u origin main --force
```
（注意：--force 会覆盖远程仓库，确保本地代码是正确的）

---

## 📝 完整命令

```bash
# 确保在项目目录
cd "/Users/zhaoyuanhao/Documents/cursor/exam index"

# 推送代码
git push -u origin main
```

---

推送成功后，告诉我，我继续指导你部署到 Vercel！🚀
