# 📤 步骤 4 和 5：创建 GitHub 仓库并上传代码

## ✅ 步骤 3 已完成！
你已经成功：
- ✅ 初始化了 Git 仓库
- ✅ 添加了所有文件
- ✅ 创建了初始提交

---

## 🚀 步骤 4：在 GitHub 上创建仓库

### 详细步骤：

1. **打开浏览器，访问 GitHub**：
   - 网址：https://github.com
   - 用你的账号登录（如果没有账号，先注册）

2. **创建新仓库**：
   - 点击右上角的 **"+"** 号（绿色按钮）
   - 选择 **"New repository"**

3. **填写仓库信息**：
   - **Repository name**（仓库名称）：
     - 建议名称：`exam-index` 或 `onion-exam-system` 或 `ued-ai-exam`
     - 只能使用小写字母、数字、连字符（-）和下划线（_）
   - **Description**（描述，可选）：
     - 例如：`洋葱学园 UED AI 技能考核系统`
   - **Public 或 Private**：
     - 选择 **Public**（公开，免费）或 **Private**（私有）
   - ⚠️ **重要**：**不要勾选**以下任何选项：
     - ❌ "Add a README file"
     - ❌ "Add .gitignore"
     - ❌ "Choose a license"
   - 保持所有选项为空（因为我们本地已经有文件了）

4. **点击绿色的 "Create repository" 按钮**

5. **创建成功后，GitHub 会显示一个页面**，上面有类似这样的内容：
   ```
   Quick setup — if you've done this kind of thing before
   
   …or push an existing repository from the command line
   
   git remote add origin https://github.com/你的用户名/仓库名.git
   git branch -M main
   git push -u origin main
   ```

---

## 📤 步骤 5：连接本地仓库到 GitHub 并上传

### 在终端中执行以下命令：

**⚠️ 重要**：把下面的 `你的用户名` 和 `你的仓库名` 替换成你实际创建的信息！

### 1. 添加 GitHub 远程仓库

```bash
git remote add origin https://github.com/你的用户名/你的仓库名.git
```

**示例**（假设你的用户名是 `yuanhao`，仓库名是 `exam-index`）：
```bash
git remote add origin https://github.com/yuanhao/exam-index.git
```

### 2. 将分支重命名为 main

```bash
git branch -M main
```

### 3. 上传代码到 GitHub

```bash
git push -u origin main
```

---

## 🔐 如果要求输入密码/用户名

执行 `git push` 时，可能会要求你：

1. **Username（用户名）**：输入你的 GitHub 用户名
2. **Password（密码）**：⚠️ **注意**：GitHub 现在不支持密码，需要使用 **Personal Access Token**

### 如何创建 Personal Access Token：

1. **访问**：https://github.com/settings/tokens
2. **点击** "Generate new token" > "Generate new token (classic)"
3. **填写信息**：
   - **Note**（备注）：例如 "My Computer" 或 "exam-index project"
   - **Expiration**（过期时间）：
     - 选择 "No expiration"（永不过期，方便）
     - 或选择自定义时间
   - **勾选权限**：至少勾选 `repo`（全部仓库权限）
4. **点击** "Generate token"（绿色按钮）
5. **复制生成的 token**：
   - ⚠️ **重要**：这个 token 只显示一次，一定要复制保存好！
   - 格式类似：`ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`
6. **在终端输入密码时，粘贴这个 token**（而不是输入密码）

---

## ✅ 验证是否成功

上传成功后：

1. **刷新你的 GitHub 仓库页面**
2. **你应该能看到所有文件**：
   - ✅ index.html
   - ✅ DEFAULT_QUESTIONS_ui.js
   - ✅ DEFAULT_QUESTIONS_visual.js
   - ✅ README.md
   - ✅ .gitignore
   - ✅ vercel.json
   - ✅ DEPLOYMENT.md
   - ✅ GITHUB_SETUP.md
   - ✅ 其他文件

如果看到了这些文件，说明上传成功！🎉

---

## 🆘 常见问题

### 问题 1：`fatal: remote origin already exists`
**解决**：说明已经添加过远程仓库了
```bash
git remote remove origin
git remote add origin https://github.com/你的用户名/你的仓库名.git
```

### 问题 2：`error: failed to push some refs`
**解决**：可能是分支名不匹配，尝试：
```bash
git push -u origin main --force
```
（注意：--force 会覆盖远程仓库，确保本地代码是正确的）

### 问题 3：`Permission denied` 或认证失败
**解决**：
- 检查用户名和仓库名是否正确
- 使用 Personal Access Token 而不是密码
- 确保 token 有 `repo` 权限

### 问题 4：想要更简单的方法？
**解决**：使用 GitHub Desktop（图形界面工具）
1. 下载：https://desktop.github.com/
2. 安装后，选择 "File" > "Add Local Repository"
3. 选择项目文件夹
4. 点击 "Publish repository"

---

## 📝 完整命令总结

假设你的 GitHub 用户名是 `yuanhao`，仓库名是 `exam-index`：

```bash
# 1. 添加远程仓库
git remote add origin https://github.com/yuanhao/exam-index.git

# 2. 重命名分支
git branch -M main

# 3. 上传代码
git push -u origin main
```

---

完成这一步后，告诉我，我继续指导你部署到 Vercel！🚀
